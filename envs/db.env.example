# Kontener PostgreSQL (docker-compose.yml, dev). Port hosta — patrz .claude/rules/dev.md.

POSTGRES_USER=homedashboard

# Wartość przykładowa tylko dla dev.
POSTGRES_PASSWORD=changeme-dev-only

POSTGRES_DB=homedashboard
